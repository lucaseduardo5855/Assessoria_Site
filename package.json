{
  "name": "z4-assessoria-system",
  "version": "1.0.0",
  "description": "Sistema completo de assessoria esportiva Z4 Performance",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd backend && npm run dev",
    "client:dev": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "setup": "npm run install:all && npm run db:setup"
  },
  "keywords": ["assessoria", "esportiva", "treino", "corrida", "musculacao"],
  "author": "Lucas Eduardo",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
